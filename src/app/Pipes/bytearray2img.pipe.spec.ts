import { Bytearray2imgPipe } from './bytearray2img.pipe';

describe('Bytearray2imgPipe', () => {
  it('create an instance', () => {
    const pipe = new Bytearray2imgPipe();
    expect(pipe).toBeTruthy();
  });
});
